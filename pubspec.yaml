name: untitled15
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.2.0-231.0.dev <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  badges: ^3.1.2
  iconsax: ^0.0.8
  firebase_core: ^2.24.2
  firebase_auth: ^4.12.0
  cloud_firestore:
  provider: ^6.0.5
  flutter_spinkit: ^5.2.0
  shared_preferences: ^2.2.2
  firebase_messaging: ^14.7.8
  firebase_analytics: ^10.7.4
  google_fonts: ^6.1.0




dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
      - lib/assets/images/profiles/
      - lib/assets/images/
      - lib/assets/carte-localisation-ville_97886-2805.jpg
  fonts:
      - family: arial
        fonts:
          - asset: lib/assets/font/Arial Rounded MT Std Bold.otf
  #       - asset: fonts/Schyler-Italic.ttf
    #        style: "bold"
    # - family: MT
      # fonts:
         # - asset: lib/font/Arial Rounded MT Regular.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
